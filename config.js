const config = {
  database: {
    host: 'localhost',
    user: 'root',        // Default XAMPP
    password: '',        // Default XAMPP (kosong)
    database: 'iot_hydroponic'
  },
  server: {
    port: 3000
  }
};

module.exports = config;